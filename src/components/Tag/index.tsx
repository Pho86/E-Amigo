import axios from "axios"
import { useState } from "react"
export default function Tag({
   active
}:{
   active: boolean
}) {
   return (
      <div className="p-2 rounded bg-primary cursor-pointer">

      </div>
   )
}